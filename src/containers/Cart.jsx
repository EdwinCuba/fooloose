import React from 'react';
import ProductsCart from '../components/ProductsCart';

const Cart = (props) => {
  return (
    <div className="Cart">
      <ProductsCart />
    </div>
  );
}
export default Cart;